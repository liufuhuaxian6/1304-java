# 共享文档管理与版本控制系统 — 实验报告

> 25–26 学年春季《JAVA 程序设计》课程大作业
>
> 项目名称：**ShareDoc · 多人协同文档编辑与版本控制系统**
>
> 提交日期：2026 年 6 月 ___ 日　|　小组成员：_______________

> **填写说明**：文中标有「✎」的表格/字段需由小组按实际情况填写（成员姓名、学号、工作量与贡献百分比、答辩分工等）。其余技术内容已按源代码实际实现撰写。

---

## 目录

1. [选题依据（含与竞品的区别）](#一选题依据)
2. [概要设计说明](#二概要设计说明)
3. [已实现的主要功能](#三已实现的主要功能)
4. [类划分](#四类划分)
5. [所使用的关键技术](#五所使用的关键技术)
6. [程序亮点](#六程序亮点)
7. [完成情况 / 问题与解决 / 待实现](#七完成情况问题与解决待实现)
8. [工作量分配表与贡献表](#八工作量分配表与贡献表)
9. [运行说明与环境要求](#九运行说明与环境要求)
10. [提交物文件清单说明](#十提交物文件清单说明)
11. [附：PPT 内容大纲建议](#十一附ppt-内容大纲建议)

---

## 一、选题依据

### 1.1 选题背景

多人协同编辑是现代办公与开发的高频需求（如腾讯文档、石墨文档、Google Docs、VS Code Live Share）。这类系统的核心难点集中在 **并发控制、实时同步、版本管理** 三方面，恰好能综合训练 Java 的多线程、网络编程、文件 I/O、面向对象设计等课程知识点。因此我们选择实现一个**面向代码 / 文本文件的轻量级共享文档系统**作为大作业。

### 1.2 与同类竞品的区别

| 维度 | 主流竞品（腾讯文档 / Google Docs） | 本项目 ShareDoc |
| --- | --- | --- |
| 协同模型 | OT / CRDT 全自由字符级协同，冲突自动合并，实现复杂 | **细粒度区间锁**：把光标/选区扩展为行级区间申请锁，**不重叠区间可多人并行编辑**，重叠则进入 FIFO 等待队列。模型介于"悲观整文档锁"与"OT 全自由"之间，**实现清晰、无复杂冲突合并、却仍支持真正的并行编辑** |
| 版本存储 | 黑盒、依赖云端数据库 | **增量补丁 + 周期快照** 的可解释存储：上传/回滚存完整快照，编辑只存修改片段，短时连续编辑自动合并，每 20 个补丁自动落一次快照保证重建成本有上界 |
| 部署形态 | 重型云服务 | **单进程零依赖**：一个可执行 jar 同时提供 API、SSE 与前端静态页，`java -jar` 或一条 `docker run` 即可启动，数据落地为本地 JSON + 文件 |
| 适用场景 | 通用富文本 | 面向**代码/配置/标记类纯文本**，内置语法高亮，更贴近开发者协作 |

> 核心差异化：我们没有照搬难以实现的 OT/CRDT，而是设计了一套**自洽且可验证的"细粒度区间锁 + 排队晋升 + 锁随编辑平移"**协同机制，在课程作业规模内同时做到"多人真并行"与"实现可控"。

---

## 二、概要设计说明

### 2.1 总体架构

系统采用**前后端分离 + 后端托管前端**的单体架构，后端按经典分层组织：

```
┌─────────────────────────────────────────────┐
│  前端（Vue 3 + 手写 CSS + highlight.js）        │
│  index.html / app.js / styles.css            │
└───────────────┬─────────────────────────────┘
                │ HTTP REST + SSE（同源）
┌───────────────▼─────────────────────────────┐
│  server 层  HttpApiServer（Javalin 路由/鉴权） │
│            DocumentEventBroker（SSE 广播）      │
├──────────────────────────────────────────────┤
│  service 层 UserService / DocumentService     │
│            LockService / VersionService        │
├──────────────────────────────────────────────┤
│  storage 层 FileStorage / VersionStorage       │
│            JsonStore（元数据原子持久化）          │
├──────────────────────────────────────────────┤
│  model 层 / util 层（实体、错误码、工具）         │
└──────────────────────────────────────────────┘
                │
        本地文件系统 data/{documents,versions,metadata}
```

### 2.2 分层职责

- **server 层**：HTTP 路由、鉴权拦截、请求/响应编解码、SSE 事件广播。只做协议适配，不含业务规则。
- **service 层**：核心业务。`DocumentService` 编排文档与保存流程、`LockService` 负责区间锁与排队、`VersionService` 负责版本存储与差异、`UserService` 负责账号与会话。
- **storage 层**：文件读写与元数据 JSON 持久化，屏蔽磁盘细节。
- **model 层**：纯数据实体与机器可读错误码（`ErrorCodes`）。
- **util 层**：密码哈希、文件名净化、ID 生成、时间格式化等无状态工具。

### 2.3 数据流（一次"保存"为例）

```
浏览器键入 → 申请/复用区间锁(POST /lock) → 本地编辑并防抖自动保存
        → PATCH /content → DocumentService.saveRange()
        → 写文件 → VersionService 记补丁/快照 → 释放锁、平移其余锁
        → DocumentEventBroker 广播 content-updated / lock-released
        → 其他在线客户端经 SSE 收到补丁并按坐标映射应用
```

### 2.4 存储设计

- `data/documents/`：当前文档正文（`{docId}_{文件名}`）。
- `data/versions/{docId}/`：历史版本文件，FULL 为完整快照、PATCH 为 `*.patch.json` 片段。
- `data/metadata/`：`users.json` / `documents.json` / `versions.json` 三份元数据索引，启动加载、变更落盘。

---

## 三、已实现的主要功能

### 3.1 账号与会话
- 登录、登出、注册（前端登录页内切换）
- 密码 **PBKDF2 加盐哈希**存储，永不返回明文
- 注册角色固定为 `USER`，杜绝越权提权
- 会话 Token **滑动过期**（默认 30 分钟）
- 修改密码

### 3.2 文档管理
- 文档列表、上传（拖拽/点击）、下载、在线预览
- **重命名、删除**（仅所有者或 `ADMIN`，删除连带清理历史版本）
- 上传文件名净化（防路径穿越）、大小限制、类型白名单
- 中文文件名下载（RFC 5987 编码）

### 3.3 协同编辑
- **细粒度区间锁**：不重叠区间多人并行编辑
- 重叠区间进入 **FIFO 等待队列**，前序锁释放后自动**晋升**
- 锁**随他人编辑长度自动平移**坐标
- 锁 **TTL 过期**（默认 10 分钟）+ 关闭页面主动释放，杜绝僵尸锁
- 防抖**自动保存** + 手动保存
- **SSE 实时同步**：锁申请/释放/排队晋升、内容局部更新、回滚、重命名/删除
- **在线状态（presence）**：实时展示在看该文档的用户

### 3.4 版本控制
- 增量版本：FULL 快照 + PATCH 片段；连续编辑自动合并；周期性快照
- 历史版本列表、版本下载
- **行级 diff**（LCS）相邻版本差异对比，红绿高亮
- **版本回滚**（生成新版本，存在活动锁时禁止），回滚后其他用户实时刷新

### 3.5 工程化
- 本地持久化，重启数据自动恢复
- 后端直接托管前端、`/api/v1/health` 健康检查
- 一键 Docker 部署
- 50 个单元/集成测试

---

## 四、类划分

> 共 27 个后端 Java 类，按包分层。前端 3 个文件。

### `com.sharedoc.model`（数据实体与错误码，10 个）

| 类 | 职责 |
| --- | --- |
| `User` | 用户实体（密码字段 `@JsonIgnore` 不外泄） |
| `Document` | 文档元数据（`currentPath` 内部路径对客户端隐藏） |
| `DocumentVersion` | 单条版本记录（含存储类型 FULL/PATCH、补丁数） |
| `VersionPatch` | 一个区间修改片段（原文/新文/区间/修订号） |
| `RangeLock` | 区间锁（基准区间、当前区间、排队信息） |
| `Response` | 统一服务层响应（成功标志 + 错误码 + 消息 + 数据） |
| `ContentUpdateResult` | 一次保存的结果聚合（版本、释放锁、平移后活动锁等） |
| `LockReleaseResult` | 释放锁结果（已释放/活动中/晋升的锁） |
| `OperationType` | 操作类型枚举（UPLOAD/EDIT/ROLLBACK…） |
| `ErrorCodes` | 机器可读错误码常量 |

### `com.sharedoc.server`（接入层，4 个）

| 类 | 职责 |
| --- | --- |
| `HttpApiServer` | Javalin 路由注册、鉴权拦截、JSON 编解码、状态码映射、静态前端托管 |
| `DocumentEventBroker` | 按文档维护 SSE 订阅者，广播事件与在线用户集合 |
| `ServerConfig` | 端口/路径/TTL/上传上限/CORS 等配置（环境变量可覆盖） |
| `ServerMain` | 程序入口，显式装配各服务 |

### `com.sharedoc.service`（业务层，4 个）

| 类 | 职责 |
| --- | --- |
| `UserService` | 登录/注册/登出/改密码、账号持久化、密码校验 |
| `DocumentService` | 文档增删改查、上传保存编排、回滚授权、每文档监视器串行化 |
| `LockService` | 区间锁申请/释放、重叠排队、晋升、TTL 过期、编辑后坐标平移 |
| `VersionService` | 版本创建/合并/快照、内容重建、行级 diff、回滚 |

### `com.sharedoc.storage`（持久化层，5 个）

| 类 | 职责 |
| --- | --- |
| `FileStorage` | 基础文件读写/复制/删除/建目录 |
| `VersionStorage` | 版本文件与补丁文件的路径构建与读写 |
| `JsonStore` | 单文件 JSON 元数据存储，**临时文件 + 原子重命名**写入 |
| `StoredUser` / `StoredDocument` | 持久化专用 DTO（与对外模型解耦，承载敏感/内部字段） |

### `com.sharedoc.util`（工具层，4 个）

| 类 | 职责 |
| --- | --- |
| `PasswordHasher` | PBKDF2 加盐哈希与校验 |
| `FileNames` | 文件名净化（防穿越、剥离控制字符与不成对代理） |
| `IdGenerator` | 顺序 ID 生成与重启后序列恢复 |
| `DateTimeUtil` | 时间格式化 |

### 前端

| 文件 | 职责 |
| --- | --- |
| `index.html` | 结构与 Vue 模板（登录/库/编辑器/版本抽屉/对话框） |
| `app.js` | Vue 3 应用逻辑：API、锁协同、客户端 OT 坐标映射、SSE、版本 |
| `styles.css` | 手写设计系统（深/浅主题、组件样式、过渡动画） |

---

## 五、所使用的关键技术

### 5.1 并发控制：细粒度区间锁
- 每个文档一把**监视器锁**（`synchronized(documentMonitor(docId))`）串行化该文档的所有写操作，避免读改写竞态；不同文档互不阻塞。
- `LockService` 内部以 `synchronized` 保护锁表，维护**活动锁列表 + FIFO 等待队列**，重叠检测、晋升、TTL 过期、编辑后坐标平移（`transformPosition`）。
- 通过 JUnit 多线程测试验证**同文档并发保存被正确串行化**。

### 5.2 实时同步：SSE + 客户端 OT
- 服务端用 **Server-Sent Events** 单向推送文档事件；`DocumentEventBroker` 用 `ConcurrentHashMap` + `CopyOnWriteArrayList` 管理订阅者，自动清理失效连接并广播在线用户。
- 前端在本地存在未保存编辑时，对收到的远端补丁做**操作变换（坐标映射 `mapPosition`）** 后再应用，避免并发编辑错位。

### 5.3 增量版本存储与高效重建
- 编辑只存补丁（`VersionPatch`），60 秒内同人连续编辑合并为一个 PATCH 版本节省空间。
- 每累计 20 个 PATCH 自动落一个 FULL 快照；`reconstructVersionContent` **从最近快照开始回放**，使任意版本重建成本由 O(全历史) 降为 O(快照间隔)。

### 5.4 安全设计
- 鉴权在 Javalin `before` 拦截器中**抛出 `UnauthorizedResponse` 中断请求管线**（仅设置状态码不会阻止 Javalin 继续执行处理器，会泄露数据——这是我们专门修复并加回归测试的点）。
- 密码 **PBKDF2（HmacSHA256，65536 轮加盐）**；登录失败统一提示不暴露用户名是否存在。
- 上传文件名净化防**路径穿越**；服务层异常只记日志、对外返回通用信息。
- 服务层返回**机器可读错误码**，HTTP 层据此映射状态码，不做脆弱的消息文本匹配。

### 5.5 持久化
- `JsonStore` 采用**临时文件 + 原子重命名**写入，进程崩溃不会留下半截文件；启动加载并将 ID 序列恢复到已有最大值之上，避免 ID 冲突。

### 5.6 框架与工具
- 后端：**Java 17、Maven、Javalin 5（内嵌 Jetty 11）、Jackson（含 JSR-310）、SLF4J**。
- 打包：**maven-shade-plugin** 生成可执行 fat jar；多阶段 **Dockerfile**。
- 前端：**Vue 3（CDN，组合式 API）、手写 CSS 设计系统、highlight.js** 语法高亮。
- 测试：**JUnit 5**，50 个用例。

---

## 六、程序亮点

1. **真正可并行的协同编辑模型**：自研"细粒度区间锁 + 排队晋升 + 锁随编辑平移"，多人编辑同一文档不重叠区间互不阻塞，兼顾实现可控与并行能力。
2. **可解释的增量版本系统**：补丁 + 周期快照 + 自动合并，既省空间又保证重建性能有上界，并支持任意版本下载、行级 diff、回滚。
3. **以安全为先的工程实践**：修复了真实存在的鉴权绕过漏洞并补回归测试，落实密码哈希、路径穿越防护、错误码体系、会话过期、CORS 白名单。
4. **干净的面向对象分层**：无静态可变共享状态，服务全部实例化 + 构造注入；对外模型与持久化 DTO 解耦；职责单一、依赖清晰。
5. **零配置可部署**：单进程同时托管 API/SSE/前端，`java -jar` 或一条 `docker run` 即起，数据本地持久化、重启不丢。
6. **精致的现代化界面**：手写深/浅主题设计系统，卡片式文档库、带行号与语法高亮的编辑器、滑出式版本时间线与行级 diff、实时在线头像，体验接近商用产品。
7. **较完善的自动化测试**：50 个单元/集成测试覆盖协同并发、锁过期、版本快照、鉴权回归、增删改权限等关键路径。

---

## 七、完成情况、问题与解决、待实现

### 7.1 完成功能情况
第三节所列功能**全部实现并通过测试**，前端经无头浏览器渲染验证，后端 50 个测试全绿。

### 7.2 完成过程中遇到的问题及解决方案

| 问题 | 解决方案 |
| --- | --- |
| Javalin `before` 拦截器仅设置 401 状态并不能中断请求，业务处理器仍执行，响应体泄露文档内容/文件字节 | 改为**抛出 `UnauthorizedResponse`** 中断管线，并新增"401 不泄露响应体"回归测试 |
| 浏览器跨域**预检 OPTIONS** 请求被鉴权拦截，导致前端所有请求失败 | 在鉴权拦截中对 `OPTIONS` 方法放行（由 CORS 配置应答） |
| 重启后内存数据丢失、`data/` 下文件成为孤儿 | 引入 `JsonStore` 元数据持久化，启动加载并恢复 ID 序列 |
| 同一用户连续编辑产生大量版本、且版本重建随历史线性变慢 | 60 秒合并补丁 + 每 20 补丁落快照，重建从最近快照回放 |
| 中文文件名上传/下载乱码与畸形 multipart 触发 500 | 下载用 RFC 5987 编码；对非 UTF-8 multipart 捕获并返回 400 |
| 早期服务层用静态变量共享状态，难以测试与并发不安全 | 重构为实例字段 + 构造注入，测试用全新实例隔离 |
| 前端编辑器语法高亮层被 `opacity:0` 与 `!important` 压制而失效 | 重构为"高亮层在底、文本透明 textarea 在上"的标准叠加方案 |

### 7.3 待实现的功能 / 未解决的问题
- 字符级 OT/CRDT 全自由协同（当前为区间锁模型，按设计取舍）。
- 多实例横向扩展与数据库后端（当前为单节点 + 本地文件）。
- 中文输入法（IME）合成输入在区间锁边界的细节体验。
- 元数据每次变更为全量重写，超大规模下存在写放大。

---

## 八、工作量分配表与贡献表

> 本节分工依据 **Git 提交记录** 与各成员交接文档 `handover-A/B/C/D.md` 总结而成。
> 姓名 / 学号请按 ✎ 填写；项目开发期间使用四个 Git 账号，对应关系如下表。
> 项目最初采用 Socket 客户端/服务端架构（`client` 包、`Request/Response` 协议），
> 中后期演进为 **HTTP REST + SSE + Web 前端**，故部分早期文件（如 `ClientApp`、`ClientHandler`）已被重构移除。

### 8.1 成员与角色对应

| 成员 | 角色 | Git 账号 |
| --- | --- | --- |
| 成员 A | 服务端架构与多线程 | `6nay01` |
| 成员 B | 文档管理与上传下载 | `LuoYiningLemon` |
| 陈尚魁 | 编辑锁与版本管理（及后期整合） | `liufuhuaxian6` |
| 成员 D | 客户端 / Web 前端与交互 | `functionx37` |

### 8.2 工作量分配表

| 成员 | 主要负责模块 | 代表性工作 |
| --- | --- | --- |
| 成员 A（6nay01） | 服务端架构、并发、HTTP API、细粒度锁、测试 | 线程池连接模型与请求分发、连接级清理；细粒度区间锁实现；HTTP API 服务端；历史版本接口；单元/集成测试 |
| 成员 B（LuoYiningLemon） | 文档管理与文件存储 | `FileStorage` 文件读写/复制/删除；`DocumentService` 上传/下载/预览/文件类型校验；客户端上传下载交互 |
| 成员 C（liufuhuaxian6） | 编辑锁与版本管理；后期系统整合 | `LockService`/`VersionService`/`VersionStorage`、初始/编辑/回滚版本与历史；后期主导安全加固、元数据持久化、增量版本与行级 diff、Web 前端重构、Docker 部署与项目文档 |
| 成员 D（functionx37） | 客户端与 Web 前端交互 | 控制台客户端菜单与登录拦截、连接管理；Web 前端页面与联调、`ServerConfig`/`ServerMain` 配套 |

### 8.3 贡献表（按百分比）

> 下表「Git 改动量」为客观统计（截至打包时），**仅作参考**；行数受重构/格式化影响，不直接等于贡献度。
> 最终百分比请小组结合实际投入（设计、调试、文档、答辩准备等）协商确定，**合计须为 100%**。✎

| 成员 | 姓名 | 学号 | Git 提交数 | Git 改动量(增/删) | 主要贡献 | 贡献百分比 |
| --- | --- | --- | --- | --- | --- | --- |
| 成员 C（liufuhuaxian6） | 陈尚魁 | 2024201564 | 12 | 6338 / 1948 | 锁与版本管理、系统整合、安全、持久化、前端重构、文档 | ✎ % |
| 成员 A（6nay01） | ✎ | ✎ | 3 | 5541 / 3227 | 服务端架构与并发、细粒度锁、HTTP API、测试 | ✎ % |
| 成员 D（functionx37） | ✎ | ✎ | 2 | 1905 / 69 | 客户端与 Web 前端交互、配置 | ✎ % |
| 成员 B（LuoYiningLemon） | ✎ | ✎ | 1 | 477 / 38 | 文档管理与上传下载、文件存储 | ✎ % |
| **合计** | | | **18** | | | **100%** |

---

## 九、运行说明与环境要求

### 9.1 运行环境
- **JDK 17**（必需）
- **Maven 3.8+**（用于编译/打包；评测可直接用已打包的 `app.jar`）
- 现代浏览器（Chrome / Edge，需支持 ES2020、`fetch keepalive`、`color-mix` CSS）
- 可选：Docker（容器化运行）

### 9.2 三种启动方式（任选其一）

**方式一：Maven 直接运行（开发）**
```bash
mvn clean compile
mvn exec:java
```

**方式二：可执行 jar（推荐评测使用）**
```bash
mvn clean package          # 生成 target/app.jar
java -jar target/app.jar
```

**方式三：Docker**
```bash
docker build -t sharedoc .
docker run -p 8082:8082 -v sharedoc-data:/data sharedoc
```

### 9.3 访问
启动后浏览器打开：

```
http://localhost:8082/
```

> 后端在同一端口直接托管前端，**无需单独启动静态服务器、不涉及跨域配置**。

### 9.4 默认账号
- `admin / 123456`（ADMIN，可回滚/删除任意文档）
- `user / 123456`（USER）

首次启动自动创建上述账号（密码以哈希形式落盘）；也可在登录页注册新账号。

### 9.5 特殊说明
- 数据持久化在运行目录的 `data/`（容器内为挂载卷 `/data`），重启自动恢复。
- 端口、存储路径、会话/锁过期、上传上限、CORS 等均可用 `SHAREDOC_*` 环境变量覆盖（详见 `README.md` 配置表）。
- 运行测试：`mvn test`。

---

## 十、提交物文件清单说明

> 提交压缩包内文件及用途说明（满足"配备电子文档所包含文件的说明"要求）。

```
shared-document-system/
├── 实验报告.md / .pdf / .docx     # 本实验报告（电子文档）
├── PPT.pptx                       # 课堂展示用 PPT
├── README.md                      # 项目说明、功能、配置、接口、测试总览
├── RUN_INSTRUCTIONS.md            # 详细运行说明与常见问题
├── frontend-api-design.md         # 前端联调 API 与错误码说明
├── Dockerfile / .dockerignore     # 容器化部署
├── pom.xml                        # Maven 构建配置
├── src/main/java/com/sharedoc/    # 后端源代码（model/server/service/storage/util）
├── src/test/java/com/sharedoc/    # 测试代码（50 个用例）
├── frontend/                      # 前端源代码
│   ├── index.html · app.js · styles.css
├── target/app.jar                 # 可运行文件（打包产物，便于直接运行）
└── data/                          # 运行时生成的数据目录（可不随源码提交）
```

| 文件/目录 | 说明 |
| --- | --- |
| `实验报告.*` | 本文档，含选题、设计、功能、类划分、关键技术、亮点、工作量等 |
| `README.md` | 面向开发者的项目总览（功能/架构/配置/接口/测试） |
| `RUN_INSTRUCTIONS.md` | 面向使用者的启动步骤与 FAQ |
| `frontend-api-design.md` | 完整 REST API、SSE 事件、错误码对照表 |
| `pom.xml` | 依赖与构建（含 shade 打包、surefire 测试） |
| `target/app.jar` | 已打包可执行文件，`java -jar app.jar` 即可运行 |
| `src/` | 全部源代码（含测试） |
| `frontend/` | 前端三件套源码 |
| `Dockerfile` | 一键容器化部署 |

---

## 十一、附：PPT 内容大纲建议

> 对应提交要求第 1 项，可据此快速制作 PPT（10–14 页）。

1. **封面**：项目名 ShareDoc、课程、小组成员与学号。
2. **选题依据**：协同编辑的价值与课程知识点契合度；与竞品的区别（区间锁模型 vs OT/CRDT）。
3. **系统演示**：登录 → 文档库 → 多人协同编辑（截图/录屏）→ 版本回滚。
4. **主要功能**：账号安全、文档管理、协同编辑、版本控制四大模块。
5. **总体架构**：分层架构图（server / service / storage / model / util）。
6. **关键技术 1**：细粒度区间锁 + 排队晋升 + 坐标平移（配示意图）。
7. **关键技术 2**：增量版本 + 周期快照 + 行级 diff。
8. **关键技术 3**：SSE 实时同步 + 客户端 OT；安全与持久化。
9. **程序亮点**：并行协同、可解释版本、安全实践、零配置部署、精致 UI、50 个测试。
10. **实验报告概要**：完成情况、遇到的问题与解决（鉴权绕过、CORS 预检、持久化、版本性能等）、待实现功能。
11. **工作量分配表与贡献表**（按百分比）。
12. **总结与展望**：收获、不足、后续可拓展方向（CRDT、数据库、多实例）。
