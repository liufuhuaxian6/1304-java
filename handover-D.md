# 成员 D 交接说明

## 1. 本次已完成内容

本次交付聚焦成员 D 负责的客户端与用户交互部分，未跨越到成员 B/C 的真实文件存储、版本落盘和回滚实现。

已完成：

- `ClientMain`
  - 保持为客户端启动入口
- `ClientConnection`
  - 增加连接状态判断
  - 增加空请求校验
  - 关闭连接时支持重复调用，避免资源重复关闭导致异常扩散
- `ClientApp`
  - 控制台菜单改为中文
  - 菜单显示当前登录用户
  - 增加登录、登出流程
  - 除登录/退出外，所有操作必须先登录
  - 所有关键输入做基础判空处理
  - 文档列表、版本列表支持格式化展示
  - 上传、下载、保存、回滚等占位能力会明确提示当前服务端仍为占位实现
  - 客户端退出前会尝试自动发送 `LOGOUT`

## 2. 当前客户端菜单行为

客户端菜单顺序如下：

1. 登录
2. 登出
3. 查看文档列表
4. 上传文档
5. 下载文档
6. 只读查看文档
7. 申请编辑权限
8. 保存文档
9. 释放编辑权限
10. 查看历史版本
11. 下载历史版本
12. 回滚版本
0. 退出

行为说明：

- 未登录时，只有登录、退出可用，其他菜单会被客户端直接拦截。
- 登录成功后，客户端会记录 `currentUsername`，后续请求自动带上该用户名。
- 登出成功后，客户端会清空当前用户状态。
- 退出时若当前仍登录，会先发送 `LOGOUT` 请求再关闭连接。

## 3. 当前请求协议使用方式

当前仍沿用项目已有的 `Request` / `Response` Java Serializable 对象协议，不新增协议字段。

### 请求发送约定

- 登录
  - `type = LOGIN`
  - `username = 输入的用户名`
  - `documentId = null`
  - `payload = 输入的密码`
- 登出
  - `type = LOGOUT`
  - `username = 当前登录用户`
- 文档列表
  - `type = LIST_DOCUMENTS`
- 上传文档
  - `type = UPLOAD_DOCUMENT`
  - `payload = 本地路径字符串`
  - 说明：这里只是成员 D 侧的占位约定，后续成员 B 可替换为真实文件内容/字节
- 下载文档
  - `type = DOWNLOAD_DOCUMENT`
  - `documentId = 用户输入的文档 ID`
- 查看文档
  - `type = VIEW_DOCUMENT`
  - `documentId = 用户输入的文档 ID`
- 申请编辑
  - `type = REQUEST_EDIT`
  - `documentId = 用户输入的文档 ID`
- 保存文档
  - `type = SAVE_DOCUMENT`
  - `documentId = 用户输入的文档 ID`
  - `payload = 备注字符串，可为空`
- 释放编辑
  - `type = RELEASE_EDIT`
  - `documentId = 用户输入的文档 ID`
- 查看版本列表
  - `type = LIST_VERSIONS`
  - `documentId = 用户输入的文档 ID`
- 下载历史版本
  - `type = DOWNLOAD_VERSION`
  - `payload = 版本 ID`
- 回滚版本
  - `type = ROLLBACK_VERSION`
  - `documentId = 用户输入的文档 ID`
  - `payload = 版本 ID`

### 响应展示约定

- `Response.success = true` 时，客户端打印 `[成功]`
- `Response.success = false` 时，客户端打印 `[失败]`
- `Response.data` 为 `Document` 时，客户端会打印文档详情
- `Response.data` 为 `List<Document>` 时，客户端会逐项打印文档列表
- `Response.data` 为 `DocumentVersion` 时，客户端会打印版本详情
- `Response.data` 为 `List<DocumentVersion>` 时，客户端会逐项打印版本列表
- 其他数据类型默认直接打印 `toString()`

## 4. 联调说明

### 与成员 A 的对接点

- 当前客户端默认连接 `localhost:8888`
- 仍使用对象流通信：`ObjectOutputStream` / `ObjectInputStream`
- 客户端每次操作默认采用“发送一个请求，等待一个响应”的同步交互模式
- 如果后续服务端需要长连接推送、异步通知或广播消息，客户端这边还需要额外改造读取循环

### 与成员 B 的对接点

- 上传功能目前只发送“本地路径字符串”，未读取本地文件字节
- 下载功能目前只发送请求并显示结果，未将返回内容写回本地文件
- 查看文档目前依赖服务端返回 `Document` 或扩展后的文本内容
- 如果成员 B 后续要支持真实文件上传下载，建议优先明确：
  - 上传 `payload` 的最终结构
  - 下载返回值的数据结构
  - 文本预览和二进制文件的区分方式

### 与成员 C 的对接点

- 历史版本列表的展示已经完成，只要服务端返回 `List<DocumentVersion>` 就能直接显示
- 历史版本下载和回滚目前仍是请求入口，不负责真实文件恢复
- 若成员 C 增加版本备注、回滚结果说明、版本内容摘要等字段，客户端可继续扩展显示

## 5. 当前已知限制

- 服务端登录逻辑仍是占位实现，当前 `UserService.login` 没有真正校验密码。
- 上传、下载、保存、查看、版本下载、回滚本质上仍依赖服务端占位返回。
- 当前环境里未发现可用 `mvn`，因此我无法在本机执行 `mvn clean compile` 验证。
- 当前客户端为控制台同步交互模式，不支持多人实时推送提示。

## 6. 手工测试建议

### 启动

1. 启动服务端：

```bash
java -cp target/classes com.sharedoc.server.ServerMain
```

2. 启动客户端：

```bash
java -cp target/classes com.sharedoc.client.ClientMain
```

### 建议测试场景

1. 不启动服务端直接启动客户端，应提示连接失败。
2. 启动客户端后，不登录直接选择文档相关操作，应提示先登录。
3. 使用 `admin / 123456` 登录，应提示成功。
4. 登录后查看文档列表，应能打印服务端返回消息，空列表时显示“暂无数据”。
5. 执行上传文档，应要求输入本地路径，并提示当前为占位上传。
6. 执行申请编辑、释放编辑、保存文档，应能正常发送请求并打印结果。
7. 执行查看历史版本，应在服务端返回列表时正确格式化显示。
8. 选择退出时，若当前处于登录状态，应先自动发送登出请求。

## 7. 后续建议

- 成员 B 完成真实上传/下载协议后，优先把客户端上传和下载从“路径占位”升级为“真实文件读写”。
- 成员 C 完成版本落盘后，优先补齐历史版本下载与回滚后的客户端提示信息。
- 成员 A 若后续加入统一错误码或日志机制，客户端可同步增强错误提示和联调诊断信息。
